{
  "name": "10thgen",
  "version": "1.0.0",
  "description": "newest cypress features",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:cr": "npx cypress open --browser=chrome --e2e"
  },
  "author": "Karel Danes",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^10.1.0"
  }
}
